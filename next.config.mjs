/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove output: 'export' if it was set
  // This allows dynamic routes and server components
}

export default nextConfig;
